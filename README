Chronos Clock Sync with RPC

// How to compile
// ====================
    // Requires packages
    sudo apt-get install rpcbind gcc build-essential
    
    // Run compile script
    Run as a normal user: "./compile.sh"


// How to compile
// ====================
    // Server
    Run as root: "./dist/server"
    
    // Client from command line
    Run as root: "./dist/client <server_address> <method[0-2]>"
                 @method: [0-Basic] [1-Christian] [2-NTP]
    
    // Client from GUI
    Run as root: "./chronos.sh"
